apiVersion: v1
kind: Secret
metadata:
  name: tams-jwt-secret
  namespace: tams
  labels:
    app.kubernetes.io/part-of: tams
type: Opaque
stringData:
  # Minimum 256-bit (32-byte) random string. Generate with:
  #   openssl rand -hex 64
  jwt-secret: CHANGE_ME_MINIMUM_64_HEX_CHARS
