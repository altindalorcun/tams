apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: tams
  labels:
    app: analysis-service
    app.kubernetes.io/part-of: tams
spec:
  type: ClusterIP
  selector:
    app: analysis-service
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
