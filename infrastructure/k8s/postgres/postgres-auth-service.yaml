apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
  namespace: tams
  labels:
    app: postgres-auth
    app.kubernetes.io/part-of: tams
spec:
  type: ClusterIP
  selector:
    app: postgres-auth
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
