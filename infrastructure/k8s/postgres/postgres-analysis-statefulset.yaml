apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-analysis
  namespace: tams
  labels:
    app: postgres-analysis
    app.kubernetes.io/part-of: tams
spec:
  serviceName: postgres-analysis
  replicas: 1
  selector:
    matchLabels:
      app: postgres-analysis
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: postgres-analysis
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: tams_analysis
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: tams-db-analysis
                  key: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tams-db-analysis
                  key: password
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(POSTGRES_USER)
                - -d
                - tams_analysis
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(POSTGRES_USER)
                - -d
                - tams_analysis
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: postgres-analysis-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-analysis-data
          persistentVolumeClaim:
            claimName: postgres-analysis-data
