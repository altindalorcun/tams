apiVersion: v1
kind: Service
metadata:
  name: postgres-rules
  namespace: tams
  labels:
    app: postgres-rules
    app.kubernetes.io/part-of: tams
spec:
  type: ClusterIP
  selector:
    app: postgres-rules
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
