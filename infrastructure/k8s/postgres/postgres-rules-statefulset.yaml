apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-rules
  namespace: tams
  labels:
    app: postgres-rules
    app.kubernetes.io/part-of: tams
spec:
  serviceName: postgres-rules
  replicas: 1
  selector:
    matchLabels:
      app: postgres-rules
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: postgres-rules
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: tams_rules
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: tams-db-rules
                  key: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tams-db-rules
                  key: password
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(POSTGRES_USER)
                - -d
                - tams_rules
            initialDelaySeconds: 20
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - $(POSTGRES_USER)
                - -d
                - tams_rules
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: postgres-rules-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-rules-data
          persistentVolumeClaim:
            claimName: postgres-rules-data
