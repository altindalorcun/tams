# Development & test dependencies (not installed in the production image).
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# Integration testing against a real Kafka broker (requires Docker).
testcontainers==4.14.2
